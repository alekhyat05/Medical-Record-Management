@model  IEnumerable<UserRole>

@{
    ViewData["Title"] = " User in Role:" + ViewData["roleName"]; ;
}

<h1>@ViewData["Title"]</h1>
<div class="row">
    <div class="col-md-4">
        <form method="post" action="EditUsersInRole">
          
            <hr />
            <div asp-validation-summary="All" class="text-danger"></div>
            <div class="form-group">
                <label>Users Not in Role</label>
               @* <input name="roleNameInput" class="form-control" />*@

                <select  name="userSelected" class ="form-control" asp-items="@ViewBag.ListofUser">
                </select>
            </div>
            <button type="submit" class="btn btn-primary">Add To Role</button>
        </form>
    </div>

</div>



@*<form method="post" asp-action="DeleteUser">*@
    <div class="card">
        <div class="card-header">
            <h2> Users from this role.</h2>
        </div>
        <table class="table">
            <thead>
                <tr>
                    <th>
                        Username
                    <th>
                        Email
                    </th>
                    <th>

                    </th>
                </tr>
                @foreach (var role in Model)
                {
                    <input type="hidden" asp-for="@role.UserId" />
                    <tr>
                        <td>
                            <label  class="form-check-label">@role.UserName</label>
                        </td>
                        <td>
                            <label class="form-check-label">@role.UserName</label>
                        </td>
                        <td>
                            @*<button type="submit" class="btn btn-primary" >Remove</button>*@
                            <a asp-action="DeleteUser" asp-route-id="@role.UserId">Delete</a>
                        </td>

                    </tr>



                }
            </thead>
            <tbody>

            </tbody>
        </table>

                <div class="card-body">
                   
                </div>
                @* <div class="card-footer">
                    <input type="submit" value="Update" class="btn btn-primary" />
                    <a asp-action="EditRole" asp-route-id="@roleId" class="btn btn-primary">Cancel</a>
                </div>*@
    </div>
@*</form>*@
